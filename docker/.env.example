# Copy this file to .env and fill in real values.
# .env is git-ignored and must never be committed.

FLASK_SECRET_KEY=change-this-to-a-random-secret-value

DB_NAME=cloudcart
DB_USER=cloudcart_user
DB_PASSWORD=change-this-password
